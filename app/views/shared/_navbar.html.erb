    <nav class="navbar has-shadow is-white">
      <div class="container">
        <div class="navbar-brand">
          <%= link_to 'drivetodev', posts_path, class: 'navbar-item' %>
          <span class="navbar-burger" id="burger" data-target="navbarMenuHeroA">
            <span></span>
            <span></span>
            <span></span>
          </span>
        </div>
        <div id="navbarMenuHeroA" class="navbar-menu">
          <div class="navbar-end has-text-primary">

            <% if current_user %>
            <%= link_to current_user.username, edit_user_registration_path, class: "navbar-item" %>
            <%= image_tag(current_user.avatar_thumbnail) %>
            <%= link_to 'new post', new_post_path, class: "navbar-item" %>
            <%= link_to 'sign out', destroy_user_session_path, method: :delete, class:"navbar-item" %>
            <% else %>
            <%= link_to 'sign In', new_user_session_path, class:"navbar-item" %>
            <%= link_to 'sign up', new_user_registration_path, class:"navbar-item" %>
            <% end %>
            <a class="navbar-item">
              about
            </a>
            <a class="navbar-item">
              contact
            </a>

          </div>
        </div>
      </div>
    </nav>
  